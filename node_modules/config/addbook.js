var mongoose = require('mongoose');
var book = require('config/bookmodel');
exports.addbook = function(id,lastdonated,donationplace,callback) {

var x = id;
if(x != null){

var newbook = new book({
  userid : id,
  lastdonated : lastdonated,
  donationplace : donationplace });

book.find({_id:id},function(err,books){

var len = books.length;

     newbook.save(function (err) {

     callback({'response':"Book Added"});

});

});
}
}
